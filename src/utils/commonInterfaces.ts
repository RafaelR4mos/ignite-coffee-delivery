export interface IChildren {
  children: React.ReactNode;
}

export interface ICoffeeData {
  id: number;
  name: string;
  tags: string[];
  description: string;
  price: number;
  coffeeImg: string;
}
